Simple app created for a Secret Santa. Currently, users sign-up and then
when everyone is signed up, the admin can run a rake task to generate
matches. There is also a mailer to email users their match.
